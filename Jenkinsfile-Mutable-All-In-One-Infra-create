pipeline {
  agent {
    label 'WORKSTATION'
  }

  options {
    ansiColor('xterm')
  }

  parameters {
    choice(name: 'ENV', choices: ['', 'dev', 'prod'], description: 'Pick Environment')
  }

  stages {

    stage('VPC') {
      steps {
        dir('vpc') {
          git branch: 'main', url: "https://github.com/vasanthi-dev/terraform-vpc.git"
          sh '''
            terraform init -backend-config=env/${ENV}-backend.tfvars
            terraform apply -auto-approve -var-file=env/${ENV}.tfvars
          '''
        }
      }
    }

    stage('ALB-n-DB') {
      parallel {

        stage('DB') {
          steps {
            dir('db') {
              git branch: 'main', url: "https://github.com/vasanthi-dev/terraform-db.git"
              sh '''
                terraform init -backend-config=env/${ENV}-backend.tfvars
                terraform apply -auto-approve -var-file=env/${ENV}.tfvars
              '''
            }
          }
        }

        stage('ALB') {
          steps {
            dir('alb') {
              git branch: 'main', url: "https://github.com/vasanthi-dev/terraform-mutable-alb.git"
              sh '''
                terraform init -backend-config=env/${ENV}-backend.tfvars
                terraform apply -auto-approve -var-file=env/${ENV}.tfvars
              '''
            }
          }
        }
      }
    }

    stage('BACKEND') {
      parallel {
    stage('CART') {
          steps {
            dir('cart') {
              git branch: 'main', url: "https://github.com/vasanthi-dev/cart.git"
              sh '''
                export TF_VAR_APP_VERSION=1.0.2
                cd terraform-immutable
                terraform init -backend-config=env/${ENV}-backend.tfvars
                terraform apply -auto-approve -var-file=env/${ENV}.tfvars
              '''
            }
          }
        }
      }
    }

    }

  post {
    always {
      cleanWs()
    }
  }

}